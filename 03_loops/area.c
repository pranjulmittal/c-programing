#include<stdio.h>
int main(){
    int a,b,area;
    printf("enter the value of a: ");
    scanf("%d",&a);
    printf("\nenter the value of b: ");
    scanf("%d",&b);
    area=a*b;
    printf("the area of rectangle is : %d",area);
    return 0;
}